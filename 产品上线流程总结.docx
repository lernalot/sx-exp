     
        理财前端开发用的工具有svn和git仓库，git上有三个仓库，epay-fund，epay-promotion和epay-fund-m,分别存储主要业务线
        (我的资产，易钱袋等)的静态资源，理财活动的静态资源，理财wap端静态资源。 
       理财有多个系统，主要前端开发的系统是athena和abel系统，svn管理分支代码，分支里前端代码主要是ftl代码（页面），
       还有Apache下一些静态资源（主要有首页js，css以及一些js组件）以及后台代码。
       当产品或者运营分配任务时，在jira里会看到相关的需求，需求如果不明确需要及时向提需求人员问清，如果有理解错误会大
       大降低开发效率，前端接到需求开始开发，从athena主干拉分支（分支命名规则为“时间-分支业务（英文）-序号/01”），主要
       用TortoiseSVN工具拉分支（务必再拉分支之前更新主干trunk从而保证拉出的分支里是最新的代码），拉完分支后检出即可，
       因为理财一般是周二和周四发布，所以开发过程中代码会不断更新，因此开发过程中会出现重新拉分支然后将之前开发的代
       码合到新分支的情况，（合代码和提交代码的时候注意是否所有代码合完，提交完）。
        git仓库需要拉分支，从master拉分支到本地，在本地分支进行开发自测通过后，合到develop分支push到远程仓库部署环境，
        登陆omad系统部署，仓库名对应服务器名，一键部署，根据服务器地址进行host绑定，如10.165.124.52 8.163.com 
        10.165.124.164 i.epay.126.net，登陆vpn后即可在网页上查看当前的改动。
       环境搭建好之后将环境host发给测试或者pd验证，如果有错误需要后续改，修正之后重启服务器，举例如athena系统，
       svn分支下的改动，如果在apache下的改动，则需重新部署athena—static服务器，如果不在Apache下的改动，则重新部署
       athena系统。如果git的改动，对应的仓库有对应的服务器，重新部署后还需要重启athena，很多时候出现git仓库代码更
       新了但是环境里的代码未更新的情况，比如js和css，调试器里新标签页打开该js或者css，更改链接后面的时间戳，刷新
       后如果代码变为最新的，那就是部署的版本还不是最新的代码版本，很有可能是重新部署了git服务器之后没有重新部署athena
       系统。       经验证通过之后上预发，上预发需要将分支发给相关开发人员合tag，每次合完tag都需要立马告知测试上预发
       从而提高发布效率，如不告知，线上即将发布版本和预发版本不同会导致风险，如果合完tag之后需求有变，在自己分支上修改
       之后，再向开发人员告知合tag，合完tag告诉测试重新上预发验证，预发验证通过后git的本地分支的改动合到master分支，
       然后push到远程仓库，然后进行线上发布。每次发布都会有一个jira（pe类型结尾）发布出来，jira里需要开发填写提测分支
       （svn分支，git上仓库对应的omad里的服务器名以及develop分支），需求备注（实现的需求，以及对应的jira），以及相关人员备注
       （验证通过后需求验证人员在问题里备注验证通过），开始发布时关注发布群的信息，发布完成之后进行线上验证，线上验证通过相关
       验证人员在发布邮件回复全部验证通过，自己的需求jira点击解决问题，在备注里备注上问题描述，影响范围，解决方案和自测是否通
       过，产品上线流程完毕。
